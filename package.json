{
  "name": "cascade-force",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Cascade Force — cinematic 2.5D run-and-gun (Three.js). Not affiliated with Konami or Contra.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "capture": "node scripts/capture-gameplay.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "playwright": "^1.61.1",
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
